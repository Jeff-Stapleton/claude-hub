{
  "name": "@claude-hub/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"(lint not configured yet)\""
  },
  "dependencies": {
    "@claude-hub/cc-config-reader": "workspace:*",
    "@claude-hub/cc-runner": "workspace:*",
    "@claude-hub/channels": "workspace:*",
    "@claude-hub/core": "workspace:*",
    "@claude-hub/orchestrator": "workspace:*",
    "@claude-hub/triggers": "workspace:*",
    "@fastify/static": "^8.0.1",
    "@fastify/websocket": "^11.0.1",
    "fastify": "^5.0.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "@types/node-cron": "^3.0.11",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
