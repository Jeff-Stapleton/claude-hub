/**
 * The renderer moved to @claude-hub/core so the pipeline package can share
 * it without a triggers dependency. Re-exported here to keep existing
 * imports and tests working.
 */
export { render } from '@claude-hub/core';
